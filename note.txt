new Set()
can't check 2 objects' equality
can check 2 strings
we can calculate hash and store in map[hash] = value
//err: we can : arr.hasOwnProperty(obj) after arr[obj] = true //results false for data:p[[1,2]]=true;p[[5,6]]=true;


